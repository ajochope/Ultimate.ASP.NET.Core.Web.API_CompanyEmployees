{
  "Logging": {
    "LogLevel": {
      "Default": "Warning"
    }
  },
  "ConnectionStrings": {
    "MyDatabase": "Server=production_server;Database=my_db;User Id=my_user;Password=my_secure_password;"
  }
  // production-specific settings
  // to run the application in production, 
  // we need to set it to the Production value on the machine we do the deployment 
  // ASPNETCORE_ENVIRONMENT environment variable
  // WINDOWS => set "ASPNETCORE_ENVIRONMENT=Production"
  // LINUX => export "ASPNETCORE_ENVIRONMENT=Production"
}